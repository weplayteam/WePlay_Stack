{
  "name": "weplay_client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.0.0",
    "react-scripts": "3.0.1",
    "react-stripe-elements": "^3.0.0",
    "stripe": "^7.1.0",
    "tailwindcss": "^1.0.3"
  },
  "scripts": {
    "start": "yarn build:tailwind & react-scripts start",
    "build": "yarn build:tailwind & react-scripts build",
    "build:tailwind": "tailwind build ./src/css/index.src.css -c tailwind.js -o ./src/index.css",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
